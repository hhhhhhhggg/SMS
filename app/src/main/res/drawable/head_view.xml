<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:useLevel="false">
    <solid android:color="@color/colorBar"/>
    <stroke
        android:width="1dp"
        android:color="@color/colorBar"/>
    <size
        android:width="20px"
        android:height="20px"/>

</shape>